import UIKit

struct Fonts {

    static let defaultFont = UIFont.systemFont(ofSize: 20.0)
    static let defaultMediumFont = UIFont.systemFont(ofSize: 20.0, weight: .medium)
    static let captionFont = UIFont.systemFont(ofSize: 16.0)
}
